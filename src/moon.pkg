import {
  "gaato/dave/internal/ffi",
}

import {
  "moonbitlang/core/env",
} for "test"

supported_targets = "js + native + wasm"

options(
  targets: {
    "js_unavailable_test.mbt": [ "js" ],
    "wasm_unavailable_test.mbt": [ "wasm" ],
  },
)
